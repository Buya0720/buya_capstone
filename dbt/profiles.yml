dbt_demo:
  outputs:
    dev: # these names can be anything you like
      dbname: bootcamp_db
      host: localhost
      pass: password
      port: 5432
      schema: public
      threads: 5
      type: postgres
      user: user
  target: dev
